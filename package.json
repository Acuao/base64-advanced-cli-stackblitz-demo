{
  "name": "node-starter",
  "version": "0.0.0",
  "start": "b64",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "base64-advanced-cli": "latest"
  }
}
